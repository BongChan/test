{
	"name": "test",
	"version": "1.0.0",
	"description": "test project",
	"main": "node-test.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		"underscore": "^1.8.3"
	}
}
